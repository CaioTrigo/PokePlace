<h1 class="title is-1">Trades</h1>
<%# <div class="columns">
  <% @my_trade_pokemons.each do |my_trade_pokemon| %>
    <%#= my_trade_pokemon.pokemon.name %>
    <%#= image_tag my_trade_pokemon.pokemon.image_url %>
  <%# end %>
<%# </div> %>

<div class="container pt-4">
  <h1 class="text-center"> Pokedex </h1>
  <div class="container d-flex align-items-center flex-wrap justify-content-center">

  <% @my_trade_pokemons.each do |my_trade_pokemon| %>
    <div class="card col-3 d-flex flex-column">
      <div class="picture">
        <%= image_tag(my_pokemon.pokemon.image_url) %>
      </div>
      <div class="content d-flex">
        <div class="col">
          <%= my_trade_pokemon.pokemon.name %>
        </div>
        <div class="col d-flex justify-content-end">
          <span>
              <%= my_trade_pokemon.pokemon.type_1 %>
          </span>
          <% unless my_trade_pokemon.pokemon.type_2.nil? %>
            <span>
              <%= my_trade_pokemon.pokemon.type_2 %>
            </span>
          <% end %>
        </div>
      </div>
    <%= link_to "trade", edit_my_pokemon_path(my_trade_pokemon), class: "btn btn-primary w-100 rounded-pill d-inline-block" %>
    </div>
  <% end %>
  </div>
</div>
